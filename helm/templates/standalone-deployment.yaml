{{- if not .Values.parseable.highAvailability.enabled }}
{{- $common := .Values.parseable.common }}
{{- $node := .Values.parseable.nodes.standalone }}
{{- $secret := .Values.parseable.secrets }}
{{- $cred := .Values.parseable.credentialFile }}
{{- $audit := .Values.parseable.auditLogging }}
{{- $env := merge (deepCopy ($node.env | default dict)) ($common.env | default dict) }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "parseable.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "parseable.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "parseable.labelsSelector" . | nindent 6 }}
  template:
    metadata:
      {{- with ($node.podAnnotations | default $common.podAnnotations) }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- with ($node.podLabels | default $common.podLabels) }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- include "parseable.labelsSelector" . | nindent 8 }}
    spec:
      {{- with .Values.parseable.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "parseable.serviceAccountName" . }}
      securityContext:
        {{- toYaml $common.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml $common.securityContext | nindent 12 }}
          image: "{{ .Values.parseable.image.repository }}:{{ .Values.parseable.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.parseable.image.pullPolicy }}
          args: [ "/usr/bin/parseable", {{ .Values.parseable.store | quote }} ]
          env:
            - name: HOSTNAME
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: metadata.name
            # common.env merged with nodes.standalone.env (node wins)
            {{- range $key, $value := $env }}
            - name: {{ $key }}
              value: {{ tpl $value $ | quote }}
            {{- end }}
            # credentials from the secret in .Values.parseable.secrets
            {{- if and $secret $secret.name $secret.keys }}
            {{- $prefix := $secret.prefix | default "" | upper }}
            {{- range $key := $secret.keys }}
            - name: {{ $prefix }}{{ $key | upper | replace "." "_" | replace "-" "_" }}
              valueFrom:
                secretKeyRef:
                  name: {{ $secret.name }}
                  key: {{ $key }}
            {{- end }}
            {{- end }}
            {{- if $cred.enabled }}
            - name: GOOGLE_APPLICATION_CREDENTIALS
              value: "{{ $cred.mountPath }}/{{ $cred.secretKey }}"
            {{- end }}
            {{- if $audit.enabled }}
            - name: P_AUDIT_LOGGER
              value: {{ $audit.p_server | quote }}
            - name: P_AUDIT_USERNAME
              value: {{ $audit.p_username | quote }}
            - name: P_AUDIT_PASSWORD
              value: {{ $audit.p_password | quote }}
            {{- end }}
          ports:
            - containerPort: 8000
          {{- with ($node.readinessProbe | default $common.readinessProbe) }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml ($node.resources | default $common.resources) | nindent 12 }}
          volumeMounts:
            {{- if $cred.enabled }}
            - name: {{ $cred.secretName }}
              mountPath: {{ $cred.mountPath }}
              readOnly: true
            {{- end }}
            - name: data-volume
              mountPath: "/parseable/data"
            - name: stage-volume
              mountPath: "/parseable/staging"
      volumes:
        {{- if $cred.enabled }}
        - name: {{ $cred.secretName }}
          secret:
            secretName: {{ $cred.secretName }}
        {{- end }}
        {{- if $node.persistence.staging.enabled }}
        - name: stage-volume
          persistentVolumeClaim:
            claimName: {{ include "parseable.fullname" . }}-staging-pvc
        {{- else }}
        - name: stage-volume
          emptyDir: {}
        {{- end }}
        {{- if $node.persistence.data.enabled }}
        - name: data-volume
          persistentVolumeClaim:
            claimName: {{ include "parseable.fullname" . }}-data-pvc
        {{- else }}
        - name: data-volume
          emptyDir: {}
        {{- end }}
      {{- with ($node.nodeSelector | default $common.nodeSelector) }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with ($node.tolerations | default $common.tolerations) }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with ($node.affinity | default $common.affinity) }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
